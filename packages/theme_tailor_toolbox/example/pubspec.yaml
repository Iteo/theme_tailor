name: theme_tailor_toolbox_example
description: A new Flutter project.

publish_to: "none"

version: 0.0.2

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  benchmark_harness: ^2.1.0
  flutter:
    sdk: flutter

  theme_tailor_annotation: ^3.0.1
  theme_tailor_toolbox:
    path: ../.

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  linteo: ^1.3.0
  theme_tailor: ^3.0.2

dependency_overrides:
  theme_tailor:
    path: ../../theme_tailor
  theme_tailor_annotation:
    path: ../../theme_tailor_annotation

flutter:
  uses-material-design: true
